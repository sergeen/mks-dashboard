import {StyledSmallCardsContainer} from "./SmallCardsContainer.styles";

const SmallCardsContainer = ({children}) => {
  return <StyledSmallCardsContainer>{children}</StyledSmallCardsContainer>;
};

export default SmallCardsContainer;
