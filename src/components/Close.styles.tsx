import styled from "styled-components";

export const StyledClose = styled.button`
  position: absolute;
  right: 28px;
  top: 28px;
  @media (max-width: 790px) {
    right: 14px;
    top: 14px;
  }
`;
